[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pricing_service"
version = "0.2.0"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.13.4",
    "pyyaml>=6.0.3",
]

[tool.hatch.build.targets.wheel]
packages = ["src/pricing_service"]
